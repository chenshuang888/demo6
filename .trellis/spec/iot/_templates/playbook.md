# Playbook 模板：从 0 到通落地剧本

> 用途：把一次成功落地的方案，写成可重复执行的步骤。

---

## 上下文签名（Context Signature，必填）

> 目的：避免“场景相似但背景不同”时照搬实现细节。

- 目标平台：`ESP32 / STM32 / ...`
- SDK/版本：`ESP-IDF x.y`、`LVGL 9.x`、`NimBLE`、`HAL`…
- 外设/链路：屏幕/触摸/串口/Wi‑Fi/BLE/外置 Flash 等
- 资源约束：Flash/RAM/PSRAM，是否允许双缓冲/大缓存
- 并发模型：回调在哪个线程/任务触发；谁是 owner（UI/协议/存储）

---

## 目标

（你要实现什么能力？成功的定义是什么？）

---

## 不变式（可直接复用）

（跨项目稳定成立的原则：例如 single-writer、回调不阻塞、read_exact 分帧、状态机单向推进等。）

---

## 参数清单（必须由当前项目提供/确认）

> 缺少关键参数时必须停手，先补参数或做最小冒烟验证。

- 端序/对齐：……
- 常量（如 `magic`/分辨率/slot 大小/payload_size）：……
- 超时/重试策略：……
- buffer/队列大小：……

---

## 前置条件

- 硬件/接口/依赖
- 版本约束（ESP-IDF/LVGL/工具链）

---

## 设计边界（避免过度设计）

- 哪些明确不做
- 哪些先做最小闭环，后续再扩展

---

## 可替换点（示例映射，不得照搬）

（哪些属于“某个工程的实现细节”：文件名/目录名/pinmap/示例参数值；给出 role→模块 的映射方式。）

---

## 分层与职责（必须写清）

- 硬件层
- 适配/胶水层（例如 LVGL port）
- 业务层

---

## 实施步骤（建议顺序）

1. ...
2. ...
3. ...

---

## 停手规则（Stop Rules）

出现以下任意一条，禁止直接照搬实施步骤，只能先补信息/先冒烟验证：

- 上下文签名不匹配（SDK/外设/资源/并发模型关键不同）
- 关键参数缺失（端序/magic/分辨率/slot/payload_size 等）
- 缺少可执行的最小验收闭环

---

## 验证顺序（必须可执行）

1. 最小冒烟：...
2. 功能验证：...
3. 压力/边界：...

---

## 常见问题（快速排障）

- 问题 → 可能原因 → 下一步查哪里
