# 抽取产物说明（中间产物）

本目录用于保存“全量通读对话记录”的抽取结果，服务于后续蒸馏：

- 先把每个对话文件从头到尾读完，并按标题/代码块切块
- 生成可回溯索引（包含行号范围、关键词命中、代码块数量等）
- 再基于索引分批写入 `spec/iot/*` 的正式知识库条目

原则：

- **不把原始对话全文复制进 spec**（避免噪声与重复），只保留索引与必要片段
- 每个蒸馏条目都应能回溯到来源文件与对应片段位置

